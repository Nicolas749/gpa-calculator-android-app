package com.example.gpacalculator;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.widget.EditText;

import java.util.ArrayList;

public class activity_edit extends AppCompatActivity {

    private EditText editMateria;
    private EditText editCodigo;
    private EditText editCreditos;
    private EditText editNota;

    @Override
    protected void onCreate(Bundle savedInstanceState) {

        super.onCreate(savedInstanceState);

        /*editMateria = (EditText) findViewById(R.id.editMateria);
        editCodigo = (EditText) findViewById(R.id.editCodigo);
        editCreditos = (EditText) findViewById(R.id.editCreditos);
        editNota = (EditText) findViewById(R.id.editNota);*/

        /*ArrayList<Materias> listaM3 = getIntent().getParcelableArrayListExtra("lista");

        Materias mate = getIntent().getParcelableExtra("lista");*/

       // editMateria.setText(mate.getNombre());






    }
}